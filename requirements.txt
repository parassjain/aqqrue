# Web UI
streamlit>=1.35.0

# LangGraph + LangChain (core)
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM providers — install whichever you use
langchain-anthropic>=0.3.0    # for LLM_PROVIDER=anthropic
langchain-openai>=0.2.0       # for LLM_PROVIDER=openai or LLM_PROVIDER=groq (OpenAI-compatible)
langchain-google-genai>=2.0.0 # for LLM_PROVIDER=google
langchain-ollama>=0.2.0       # for LLM_PROVIDER=ollama

# Data processing
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.0

# Visualization
plotly>=5.20.0
kaleido>=0.2.1

# Utilities
python-dotenv>=1.0.0
